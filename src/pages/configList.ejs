<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <title>S3 Explorer - Configurations</title>
</head>
<body>
    <header>
        <h1>Configuration Lists</h1>
        <a href="/"><button>Home</button></a>
        <a href="/bucket"><button>Access Buckets</button></a>
    </header>
    <fieldset>
        <legend>Current Configuration</legend>
        <a href="/config/save"><button>Save</button></a>
        <a href="/config"><button>Edit</button></a>
        <table>
            <tr><th><label for="sessionName">sessionName</label></th><td><input readonly disabled type="text" value="<%= session.aws.sessionName %>" name="sessionName" /></td></tr>
            <tr><th><label for="region">region</label></th><td><input readonly disabled type="text" value="<%= session.aws.region %>" name="inputRegion" /></td></tr>
            <tr><th><label for="accessKeyId">accessKeyId</label></th><td><input readonly disabled type="text" value="<%= session.aws.accessKeyId %>" name="inputAccessKeyId" /></td></tr>
            <tr><th><label for="secretAccessKey">secretAccessKey</label></th><td><input readonly disabled type="text" value="<%= session.aws.secretAccessKey %>" name="inputSecretAccessKey" /></td></tr>
        </table>
    </fieldset>
    <fieldset>
        <legend>Saved Configurations</legend>
        <table>
            <% if (configList) { %>
                <% for (var i = 0; i < configList.length; i++) { %>
                <tr>
                    <td><input type="text" readonly disabled value="<%= configList[i] %>" /></td>
                    <td><a href="/config/load?config=<%= configList[i] %>"><button>Load</button></a></td>
                    <td><a href="/config/delete?config=<%= configList[i] %>"><button>Delete</button></a></td>
                </tr>  
              <% } %>
            <% } %>
        </table>
    </fieldset>
</body>
</html>